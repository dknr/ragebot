# Build output
/ragebot

# Runtime state
*.db
*.db-shm
*.db-wal

# Secrets: real config and environment values must never be committed
config.json

# Sentiment model build artifacts (regenerated by `make`)
blobs/
work/
.venv/
libtokenizers.a

# NEAT! reaction GIFs are local-only; the sentinel README stays committed
assets/*.gif

# Crush session data
/.crush/
refs/
